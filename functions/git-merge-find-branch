# -*- sh -*-

# <Function Class: git

git-merge-find-branch ()
{
    echo "${FUNCNAME[0]} \"$@\" ## $(date +%Y%m%d%H%M%S)" >> ${TILDAE:-$HOME}/personal/data/funcsaudit ## This is audit

    git config -f ${TILDAE:-$HOME}/.gitconfig.merge_audit --list | grep "$@"
}


