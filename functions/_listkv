# -*- sh -*-

# <Function Class: util>

_listkv ()
{

    declare -n array_alias="$1"
    for i in "${!array_alias[@]}"
    do
        printf "%s %s\n" "$i" "${array_alias[$i]}"
    done
    return $(true)
}


