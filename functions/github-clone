# -*- sh -*-
# shellcheck shell=bash

# <Function Class: ../bin/git>

github-clone ()
{
    if [ -z "$1" ]
    then
        echo "Need org and repo to clone"
        return 1
    else
        git-clone --proto github "$@"
        return $?
    fi
}
