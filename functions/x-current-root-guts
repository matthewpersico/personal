# -*- sh -*-

# <Function Class: X>

x-current-root-guts ()
{
    x-is-available;
    if [ ! "$?" = "${TRUE}" ]
    then
        return $FALSE
    fi
    echo $(xwininfo -root | grep -i "$1:" | sed 's/.* //')
    return $TRUE
}


