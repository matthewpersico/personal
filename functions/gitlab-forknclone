# -*- sh -*-
# shellcheck shell=bash

# gitlab-forknclone

# <Function Class: git>
# <Function Justification: cd>

# DO NOT REPLACE WITH AN ALIAS. The alias doesn't make it to subshells.

gitlab-forknclone ()
{
    gitremote-forknclone gitlab "$@"
}
autotrack gitlab-forknclone "$0"
