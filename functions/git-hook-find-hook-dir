# -*- sh -*-

# <Function Class: git

git-hook-find-hook-dir ()
{
    local dotgit=$(git-find-git-dir)
    if [ -n "$dotgit" ] && [ -d $dotgit/hooks ]
    then
        echo $dotgit/hooks
        return $OK
    else
        echo
        return $NOT_OK
    fi
}


