#!/usr/bin/env bash

# <Function Class: bash arrays>
# <Function Justification: env mod>

asplit ()
{
    if [ "$1" = '-' ]
    then
        shift
        asplit-to-stdout "$@"
    else
        asplit-to-array "$@"
    fi
    return $?
}
