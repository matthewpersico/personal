# -*- sh -*-

# <Function Class: git

git-hold-list ()
{
    ##@@ 0001||group||git-hold-list||show all entries in the git hold stash
    local ghf=$(git-hold-find)
    [ -n "$ghf" ] && ls -la $(ghf) | sed "s|/.*.githold/||"
}
