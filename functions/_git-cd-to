# -*- sh -*-

# <Function Class: git>

_git-cd-to ()
{
    local tgt=$(realpath $1)
    local src=$(pwd)
    builtin cd $tgt && apush GITFUNCS_OLDPWDS "$src" ## Push only if we can get
                                                     ## to the target
    return $?

}


