# -*- sh -*-
# shellcheck shell=bash

# <Function Class: util>
# <Function Justification: none>

is-redirected ()
{
    if [ -t "$1" ]
    then
        return "$NOK"
    else
        return "$OK"
    fi
}
