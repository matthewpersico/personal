# -*- sh -*-

# <Function Class: git>
# <Function Justification: none>

git-find-conflicts ()
{
    ##@@ 0001||group||git-find-conflicts||search git repo files for the opening conflict marker (<<<<<<)

    local opts='-l'
    if [ "$1" != '' ]
    then
        opts="$@"
    fi
    git grep $opts -E '^<<<<<<<' | sort -u
}
