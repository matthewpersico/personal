#!/usr/bin/env bash

# <Function Class: util>
# <Function Justification: calls func-echo>

func-yesno ()
{
    local prompt_text
    prompt_text="$(func-echo -n "$1")"
    shift
    yesno "$prompt_text" "$@"
}
