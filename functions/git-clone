# -*- sh -*-
# shellcheck shell=bash

git-clone ()
{
    local pipename
    pipename=$(mktemp -u --suffix .git-clone.fifo)
    mkfifo "$pipename"
    _git-clone-guts --pipe "$pipename" "$@"
    gitgo "$(cat "$pipename")"
    \rm -f "$pipename"
}
