# -*- sh -*-

# <Function Class: git

git-root ()
{
    echo "${FUNCNAME[0]} \"$@\" ## $(date +%Y%m%d%H%M%S)" >> ${TILDAE:-$HOME}/personal/data/funcsaudit ## This is audit
    ##@@ 0001||group||git-root||Print the root of the current repo

    git rev-parse --show-toplevel

    return $?
}


