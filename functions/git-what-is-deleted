# -*- sh -*-

# <Function Class: git>
# <Function Justification: none>

git-what-is-deleted ()
{
    ##@@ 0001||group||git-what-is-deleted||

    if [ -n "$1" ]
    then
        echo 'Files deleted, both staged and not staged for commit'
    else
        $DEBUG_GIT_STATUS_PARSE $(which git-status-parse) --deleted
    fi
}
