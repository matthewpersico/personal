# -*- sh -*-

# <Function Class: git>
# <Function Justification: none>

git-strip-repo ()
{
    ##@@ 0001||group||git-strip-repo||Remove the text '(repo)' from the argument.

    echo $1 | sed 's/(repo)//'
}
