# -*- sh -*-

# <Function Class: util>
# <Function Justification: yes - assumes $0>

script-usage ()
{
    ##@@ none||none||script-usage||Reads '$0' for a __USAGE__ tag and prints everything after that.
    printf '%s\n' "$(sed '0,/^__USAGE__$/d' $0)"
}
