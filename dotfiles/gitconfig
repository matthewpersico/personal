[alias]
    st = status
    stnou = status -uno
    stl = status .
    co = checkout
    br = branch
    branches = branch
    unstage = reset HEAD --
    cdiff = diff --cached

[push]
    default = simple
; Uncomment the following to force all pushes to require repo and branch args.
; [remote]
;     pushdefault = PUSHREMOTE_NOT_CONFIGURED

[merge]
    conflictstyle = diff3

[core]
    pager = less -RF
    excludesFile = /home/mpersico5/.gitignore
    editor = emacs-for-git

[user]
    name = Matthew Persico
    email = MPERSICO5@bloomberg.net

[color]
    branch = auto

[color "status"]
    added = green black
    changed = yellow black
    untracked = red black bold

[color "diff"]
    frag = cyan bold
    old = red
    new = green bold

[http "https://github.com"]
     proxy = https://devproxy.bloomberg.com:82
     sslVerify = true
     sslCAInfo = /home/mpersico5/personal/dotfiles/Bloomberg_LP_CORP_CLASS_1_ROOT_CA.cer

[http "https://bitbucket.org"]
     proxy = https://devproxy.bloomberg.com:82
     sslVerify = true
     sslCAInfo = /home/mpersico5/personal/dotfiles/Bloomberg_LP_CORP_CLASS_1_ROOT_CA.cer

[init]
    templatedir = /home/mpersico5/.git_template
