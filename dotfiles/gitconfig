[user]
    name = Matthew Persico
    email = MPERSICO5@bloomberg.net
[alias]
    st = status
    stnou = status -uno
    stl = status .
    co = checkout
    br = branch
    branches = branch
    unstage = reset HEAD --
[push]
    default = simple
; Uncomment this to force all pushes to require repo and branch args
; [remote]
;     pushdefault = PUSHREMOTE_NOT_CONFIGURED

[color]
    branch = auto
[color "status"]
    added = green black
    changed = yellow black
    untracked = red black bold 
[color "diff"]
    frag = cyan bold
    old = red
    new = green bold
[merge]
    conflictstyle = diff3

[http "https://github.com"]
     ;proxy = https://devproxy.bloomberg.com:82
     proxy = https://devproxyfarm.bloomberg.com:82
     sslVerify = false

[core]
    pager = less -RF
    excludesFile = /home/mpersico5/.gitignore
[init]
    templatedir = /home/mpersico5/.git_template
