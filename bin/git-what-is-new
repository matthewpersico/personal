#!/usr/bin/env bash

# git-what-is-deleted

##@@ 0001||group||git-what-is-deleted||

if [ -n "$1" ]
then
    echo 'Files that are new'
else
    $DEBUG_GIT_STATUS_PARSE $(which git-status-parse) --new
fi
