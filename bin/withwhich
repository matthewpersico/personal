## -*- sh -*-
set -x
lookfor=$1
shift
found=$(which $lookfor)
if [ -n "$found" ]
then
    orig="$@"
    repl=$(echo "$@" | sed "s|%|$found|")
    if [ "$orig" = "$repl" ]
    then
        $@ $found || exit 1
    else
        $repl || exit 1
    fi
fi
