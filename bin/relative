## -*- sh -*-

cmd=$1; shift

while [ ! -f $1 ] && [ ! -d $1 ]
do
    opts="$opts $1"
    shift
done

src=$1; shift
tgt=$1; shift
yesno "$cmd $opts $src $tgt/$src: Are you sure" n $cmd $opts $src $tgt/$src
