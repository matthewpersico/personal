#!/usr/bin/env bash

# git-what-is-ignored

##@@ 0001||group||git-what-is-ignored||

if [ -n "$1" ]
then
    echo 'Files ignored'
else
    $DEBUG_GIT_STATUS_PARSE "$(which git-status-parse)" --ignored
fi
