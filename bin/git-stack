## -*- sh -*-
if [ -z "$1" ]
then
    git stash list
elif [ "$1" = 'push' ]
then
    shift
    git stash save "$@"
elif [ "$1" = 'one' ] || [ "$1" = 'some' ]
then
    shift
    git stash --patch "$@"
else
    git stash "$@"
fi
