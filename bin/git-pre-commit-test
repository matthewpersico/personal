#!/usr/bin/env bash

echo "$(date): git-pre-commit-test" >> "$HOME/.gitaudit"

# git-pre-commit-test

declare hookroot
hookroot="$(git worktree get-root)"
[ -z "$hookroot" ] && hookroot="$(git root)"
"$hookroot/.git/hooks/pre-commit" "$@"
