#!/usr/bin/env bash

summary=0
if [ "$1" = '-y' ]
then
    summary=1
    shift;
fi

while read -r func
do
    autoload -l "$func"
    ((summary+=1))
done < "$1"

((summary)) && echo auto-unshimmed: $summary
return ## We dot this script in, not exit
