#!/usr/bin/env bash

summary=0
if [ "$1" = '-y' ]
then
    summary=1
    shift;
fi

for f in $(cat $1)
do
    autoload -l $f
    ((summary+=1))
done

((summary)) && echo auto-unshimmed: $summary
return ## We dot this script in, not exit
