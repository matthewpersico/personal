# -*- sh -*-
# shellcheck shell=bash

# gcc.mopenv

GCC_VERSION=$(gcc --version | head -1 | perl -ane '@x=split(/\./,$F[-1]);print $x[0]')
if [[ -z $GCC_VERSION ]];then
    cmd-echo --id gcc.mopenv --ec "Cannot set GCC_VERSION"
else
    export GCC_VERSION
fi
