#!/usr/bin/env bash

# git-what-is-unmodified

##@@ 0001||group||git-what-is-unmodified||

if [ -n "$1" ]
then
    echo 'Files unaltered, but tracked'
else
    $DEBUG_GIT_STATUS_PARSE $(which git-status-parse) --unmodified
fi
