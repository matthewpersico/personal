#!/usr/bin/env bash

declare -a opts
while [ ! -f "$1" ] && [ ! -d "$1" ]
do
    opts+=("$1")
    shift
done

if (($# != 2))
then
    echo cp-reverse "${opts[@]}" target source
    exit 1
fi

yesno "cp ${opts[*]} $2 $1: Are you sure" n cp -v "${opts[@]}" "$2" "$1"
