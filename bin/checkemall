#!/usr/bin/env bash

declare -a repos
repos=(
    "$HOME/personal"
    "$HOME/.emacs.d/taps/$USER"
    "$HOME/examples"
    "$HOME/.git_template"
)

for repo in "${repos[@]}"
do
    (
        cd "$repo" || exit 1
        echo "**** $(pwd) ****"
        git status
        echo
    )
done
