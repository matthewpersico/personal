# -*- sh -*-

# makepstree

declare os=$(uname -o)
case $os in
    Cygwin )
        echo Using /usr/bin/pstree
        ln -s /usr/bin/pstree ${TILDAE:-$HOME}/personal/bin/pstree
        ;;
    * )
        echo Creating local pstree
        builtin cd ${TILDAE:-$HOME}/personal/src/pstree
        make
        builtin cd -
        ;;
esac
