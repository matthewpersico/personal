# -*- sh -*-

# autoloadfuncs

auditfuncs-init
## We use this construction to find controlfuncs_build because we load this
## before we set up personal path stuff.
. $(dirname $AUTOLOADFUNCS_FILE)/controlfuncs_build $AUTOLOADFUNCS_FILE

## The guts are elsewhere so I can keep them in sync with the open source version.
gutsfile=$(dirname $AUTOLOADFUNCS_FILE)/autoload

. $gutsfile

## Here instead of in $gutsfile so $gutsfile is clean for open source consumption.
safe_func_export --file $gutsfile

auditfuncs-end
