# -*- sh -*-
# shellcheck shell=bash

# env-vo - env, variables only

env | perl -e '
use strict;
use warnings;
while(<>) {
    if ( /^[0-9a-zA-Z_+%=-]+\(\)/ .. /^}$/ ) {
        next;
    }
    print $_;
}'
