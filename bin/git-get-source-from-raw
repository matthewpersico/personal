# -*- sh -*-
# shellcheck shell=bash

# git-get-source-from-raw

# Print the steps instead of trying to code it.

echo o Clone the raw git locally.
echo o Do a 'git ls-remote' on the remote repo to get branches and tags.
echo o Pick one of the branches or tags to checkout.
echo o May need some --remote options on that checkout.
