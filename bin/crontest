echo -n CRON is
echo $CRON
echo
echo PATH is
listpath
echo
echo args are "${@]}"
echo
echo Environment variables are
env | sort | grep -E '^[A-Za-z_]+='
echo
echo Loaded functions are are
env | sort | grep -E '^BASH'
exit 1
